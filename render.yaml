services:
  - type: web
    name: tripperbot-rasa
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      rasa run -m models --enable-api --cors "*" --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.19
